#!/usr/bin/perl

use Modern::Perl;
use Game_area;
use Location_object;
#use Mojo::Log;

#say my $area = Game_area->new( '1', '500', '500' );
#say $area->get_id( );
#say my $area = Game_area->db_read_area( '11' );
#say $area->get_size( );
#say Game_area->list_of_areas();

#my $planet = Location_object->new( 'planet', 'Earth', '120', '130','2' );
#say my $planet = Location_object->db_read_object( '3' );
#say $planet->set_area_number( '3' );
#say Location_object->list_of_objects( );
#$planet->get_info();

#my $log = Mojo::Log->new( path => 'log.log' );

#say my $area = Game_area->db_read( '1' );
#say $area->( );
#say $area->{size_y};